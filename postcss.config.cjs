// postcss.config.js

module.exports = {
  plugins: {
    tailwindcss: {},   // Ensures Tailwind CSS processing
    autoprefixer: {},  // Adds vendor prefixes to CSS properties for browser compatibility
  },
};